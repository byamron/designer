{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Designer. Only the IPC commands the frontend calls today, plus the event-stream channel, are reachable. No fs/shell/dialog permissions — those arrive in Phase 13.E.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
