{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for Designer. IPC + event channel + native folder picker (used by the create-project flow). No fs/shell read/write permissions — Designer touches the disk only through validated Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open"
  ]
}
