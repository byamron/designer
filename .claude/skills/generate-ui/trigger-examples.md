# generate-ui — trigger examples

Per plan §13.6. 10 example prompts: 5 should-fire, 5 should-not-fire.

## Should fire

1. "Build a settings page with sections for profile, notifications, and billing."
2. "Add a notifications dropdown to the top-right of the header."
3. "Generate a confirmation dialog for destructive actions."
4. "Extend Card with a variant that shows a thumbnail on the left."
5. "Refactor the pricing section to stack vertically on narrow widths."

## Should NOT fire

1. "Audit the checkout page for a11y issues." *(→ audit-a11y)*
2. "Check if my tokens are correct in this file." *(→ enforce-tokens)*
3. "List the components we have." *(→ check-component-reuse; also serves as a reference query)*
4. "Propagate the new --space-4 value across every layout." *(→ propagate-language-update)*
5. "Initialize Mini in this repo." *(→ elicit-design-language)*

## Phase 3 audit result

`<TO BE FILLED IN during Phase 3 trigger audit per §13.6.>`

## Phase 5 observations

`<Append notes on observed misses or false positives during dogfooding.>`
